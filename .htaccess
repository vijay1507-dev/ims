<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent infinite loop by checking if the request is already routed to /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
